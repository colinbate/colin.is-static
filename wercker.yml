box: ubuntu
build:
  steps:
    - arjen/hugo-build:
        version: "0.14"
        flags: --disableSitemap=true
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: colin.is
        basedir: public